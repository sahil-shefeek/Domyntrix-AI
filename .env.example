# Copy this file to .env and adjust values before running docker compose up

# Redis configuration
REDIS_HOST=redis

# TFLite model interpreter pool size (increase for higher concurrency)
MODEL_POOL_SIZE=4

# Timeout in seconds for the Tranco list download at startup
TRANCO_DOWNLOAD_TIMEOUT_SECONDS=60
